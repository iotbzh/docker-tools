<services>
  <service name="tar_scm">
    <param name="url">https://github.com/iotbzh/docker-tools.git</param>
    <param name="scm">git</param>
    <param name="version">0.1</param>
    <param name="revision">master</param>
    <param name="extract">conf.d/packaging/rpm/docker-tools.spec</param>
    <param name="extract">conf.d/packaging/arch/PKGBUILD</param>
  </service>
  <service name="recompress">
    <param name="compression">gz</param>
    <param name="file">*.tar</param>
  </service>
  <service name="set_version" />
</services>
